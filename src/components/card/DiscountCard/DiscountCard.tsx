import {View, Text} from 'react-native';
import React from 'react';
import {DiscountCardProps} from './DiscountCard.types';

const DiscountCard = ({}: DiscountCardProps) => {
  return (
    <View>
      <Text>DiscountCard</Text>
    </View>
  );
};

export default DiscountCard;
