import Icon from './Icon';

const IconMeta = {
  title: 'Icon',
  component: Icon,
};

export default IconMeta;

export const Basic = {
  args: {
    name: 'cart',
    style: {
      fontSize: 45,
      color: '#bdbdbd',
    },
  },
};
