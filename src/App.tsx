import {SafeAreaView, Text} from 'react-native';
import React from 'react';

type Props = {};

const App = (props: Props) => {
  return (
    <SafeAreaView>
      <Text>App</Text>
    </SafeAreaView>
  );
};

export default App;
